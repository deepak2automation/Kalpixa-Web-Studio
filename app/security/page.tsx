import type { Metadata } from 'next'; import { LegalPage } from '@/components/LegalPage'; import { site } from '@/lib/site';
export const metadata:Metadata={title:'Security',description:'Kalpixa’s website security controls and responsible disclosure channel.',alternates:{canonical:'/security'}};
export default function SecurityPage(){return <LegalPage eyebrow="Standards" title="Security and responsible disclosure" intro="Security is treated as an operating practice: minimize data, restrict access, verify behavior and make failures safe."><h2>Website controls</h2><p>The rebuilt website limits third-party code, applies restrictive browser headers, bounds form input, uses Netlify’s form detection and spam controls, keeps submissions behind authenticated site administration and exposes no public lead-read endpoint.</p><h2>What not to send</h2><p>Do not submit passwords, payment-card details, government identifiers, health information or confidential client data through the project form.</p><h2>Report a vulnerability</h2><p>Email <a href={`mailto:${site.email}?subject=Security%20report`}>{site.email}</a> with “Security report” in the subject. Include the affected URL, reproduction steps and impact. Do not access other people’s data, degrade the service, use social engineering or publish details before we have had a reasonable opportunity to investigate.</p><h2>Response</h2><p>We will acknowledge a credible report, triage it and communicate remediation progress where possible. This page does not create a bug-bounty program or authorize unlawful testing.</p></LegalPage>;}
