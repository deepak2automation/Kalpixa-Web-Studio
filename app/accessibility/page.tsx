import type { Metadata } from 'next'; import { LegalPage } from '@/components/LegalPage'; import { site } from '@/lib/site';
export const metadata:Metadata={title:'Accessibility Statement',description:'Kalpixa’s accessibility commitments, current measures and feedback channel.',alternates:{canonical:'/accessibility'}};
export default function AccessibilityPage(){return <LegalPage eyebrow="Standards" title="Accessibility statement" intro="We want this website to be usable by as many people as possible, across devices, input methods and assistive technologies."><h2>Our target</h2><p>This rebuild is designed and reviewed against WCAG 2.2 Level AA principles. That is a continuing target, not a claim that every future content change will remain perfect without review.</p><h2>Measures included</h2><p>The site uses semantic landmarks and headings, a skip link, keyboard-operable controls, visible focus states, labelled form fields, text alternatives, responsive layouts, reduced-motion support and color combinations selected for readable contrast.</p><h2>Known limitations</h2><p>No known material accessibility blocker is intentionally left in this release. Automated checks cannot prove full accessibility, so keyboard, zoom, content and assistive-technology feedback remain important.</p><h2>Feedback and alternatives</h2><p>If anything prevents you from accessing information or submitting an enquiry, email <a href={`mailto:${site.email}`}>{site.email}</a> or call <a href={`tel:${site.phoneHref}`}>{site.phoneDisplay}</a>. Tell us the page and the format you need; we will provide a reasonable alternative and investigate the issue.</p></LegalPage>;}
