import type { Metadata } from 'next';
import { PageHero } from '@/components/PageHero';
import { site } from '@/lib/site';
import { ContactForm } from './ContactForm';
export const metadata:Metadata={title:'Start a Project',description:'Tell Kalpixa what needs to change. Request a confidential website strategy, design, SEO or care conversation.',alternates:{canonical:'/contact'}};
export default function ContactPage(){return <main id="main-content"><PageHero eyebrow="Start a conversation" title="Tell us what is not working—and what better would make possible." lead="You do not need a finished brief. Share the business context, the friction and the outcome you care about. We will respond with useful next questions."/><section className="contact-layout shell"><div className="contact-aside"><h2>Direct contact</h2><a href={`mailto:${site.email}`}>{site.email}</a><a href={`tel:${site.phoneHref}`}>{site.phoneDisplay}</a><a href={site.whatsapp}>WhatsApp →</a><hr/><h3>What happens next</h3><ol><li>We review the context personally.</li><li>We reply within two working days.</li><li>If there is a fit, we schedule a focused discovery call.</li></ol><p>No automated sales sequence. No obligation.</p></div><ContactForm/></section></main>;}
